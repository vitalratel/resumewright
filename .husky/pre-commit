#!/bin/sh

echo "🔍 Running type check..."
pnpm typecheck || {
  echo "❌ Type check failed. Fix errors or use --no-verify to skip."
  exit 1
}

echo "🔍 Running linter..."
pnpm lint || {
  echo "❌ Lint failed. Fix errors or use --no-verify to skip."
  exit 1
}

echo "✅ Pre-commit checks passed!"
